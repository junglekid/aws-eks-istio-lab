---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: kiali
  namespace: flux-system
spec:
  releaseName: kiali
  targetNamespace: kiali-operator
  interval: 50m
  chart:
    spec:
      chart: kiali-operator
      sourceRef:
        kind: HelmRepository
        name: kiali
        namespace: flux-system
  values:
    # resources:
    #   requests:
    #     cpu: 100m
    #     memory: 128Mi
    #   limits:
    #     memory: 512Mi
    # replicaCount: 6
    cr:
      create: true
      namespace: istio-system
      spec:
        istio_labels:
          app_label_name: "app.kubernetes.io/name"
          version_label_name: "app.kubernetes.io/version"
        # deployment:
        #   ingress:
        #     # default: additional_labels is empty
        #     additional_labels:
        #       ingressAdditionalLabel: "ingressAdditionalLabelValue"
        #     class_name: "nginx"
        #     # default: enabled is undefined
        #     enabled: false
        #     # default: override_yaml is undefined
        #     override_yaml:
        #       metadata:
        #         annotations:
        #           alb.ingress.kubernetes.io/actions.ssl-redirect: '{"Type": "redirect", "RedirectConfig":
        #             { "Protocol": "HTTPS", "Port": "443", "StatusCode": "HTTP_301"}}'
        #           alb.ingress.kubernetes.io/certificate-arn: "arn:aws:acm:us-west-2:704855531002:certificate/d355e1ad-d185-4ae8-b536-51967b156f65"
        #           alb.ingress.kubernetes.io/listen-ports: '[{"HTTP": 80}, {"HTTPS": 443}]'
        #           alb.ingress.kubernetes.io/scheme: internet-facing
        #           alb.ingress.kubernetes.io/ssl-redirect: "443"
        #           alb.ingress.kubernetes.io/target-type: ip
        #           external-dns.alpha.kubernetes.io/hostname: "kiali.dallin.brewsentry.com"
        #       spec:
        #         rules:
        #         - http:
        #             paths:
        #             - path: "/kiali"
        #               pathType: Prefix
        #               backend:
        #                 service:
        #                   name: "kiali"
        #                   port:
        #                     number: 20001
        external_services:
          prometheus:
            # Prometheus service name is "metrics" and is in the "telemetry" namespace
            url: "http://kube-prometheus-stack-prometheus.monitoring:9090"
          grafana:
            auth:
              # ca_file: ""
              # insecure_skip_verify: false
              password: "Grafana&Git0ps"
              # token: ""
              type: "none"
              # use_kiali_token: false
              username: "kiali"
            enabled: true
            in_cluster_url: 'http://kube-prometheus-stack-grafana.monitoring'
            url: 'https://grafana.BASE_DOMAIN_NAME'
            dashboards:
            - name: "Istio Service Dashboard"
            - name: "Istio Workload Dashboard"
            - name: "Istio Mesh Dashboard"
            - name: "Istio Control Plane Dashboard"
            - name: "Istio Performance Dashboard"
            - name: "Istio Wasm Extension Dashboard"
          istio:
            component_status:
              components:
              - app_label: "istiod"
                is_core: true
                is_proxy: false
              - app_label: "istio-ingressgateway"
                is_core: true
                is_proxy: true
                namespace: "istio-ingress"
              enabled: true
          tracing:
            enable: false
